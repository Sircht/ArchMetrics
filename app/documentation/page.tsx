export default function DocumentationPage(){return <main className="mx-auto max-w-4xl px-4 py-12 sm:px-6 lg:px-8"><h1 className="font-display text-5xl font-bold text-gradient">Documentation</h1><div className="prose prose-invert mt-8 max-w-none"><p>ArchMetrics calculators expose input assumptions, formula outputs, validation status, and export guidance for professional review.</p><h2>Stair methodology</h2><p>The flagship stair tool computes riser count, riser height, tread depth, total run, slope angle, and Blondel comfort validation while surfacing NBR 9050 accessibility guidance.</p><h2>Deployment</h2><p>Set PostgreSQL in <code>DATABASE_URL</code>, configure <code>NEXTAUTH_SECRET</code>, run Prisma migrations, then deploy to Vercel or any Node-compatible platform.</p></div></main>}
